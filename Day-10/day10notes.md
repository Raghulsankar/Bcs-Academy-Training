# Optional Chanining operator

- optional chaining -> (?.)

# Nullish coalesing operator

- nullish coalesing -> (??)

# ternary operator

ternary opertor -> condition ? value1: value 2

- condition,value1,value2 these are operands

> can assign it an variable so it is called 'expression'

# immutable

> immutablity reduce the line of code for reading

- "spread" is the way for immutable to the array

# Array methods

three things must be in Array method

🧾 Mutation Table
Method Mutates Array? yes- mutable, No - immutable
fill() ✅ Yes
push() ✅ Yes
pop() ✅ Yes
splice() ✅ Yes
map() ❌ No
filter() ❌ No
slice() ❌ No
join() ❌ No

# Map Rules

1. it always returns copy of array
2. sourceArray.length === outputArray.length -> True
3. Transforms data type

> whenever you will change one data type to another datatype means you will use " map ()" method.
> map will be an HOF function (Higher Order Function)

# Filter()

1. it always returns copy of array
2. sourceArray.length >= outputArray.length -> true
3. input datatype === output data type.
