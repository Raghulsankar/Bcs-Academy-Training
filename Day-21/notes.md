# day 21

# Windows methods:

- Prompt
- Alert
- Confirm

# CallBack Func:

- HOF arguments takes the function that’s called.
- Set like a time argument that is called async callbackfn
- Only take function argument that is called sync callbackfn

# JAVA SCRIPT

- Created live script branded ink (10 Days spend created)
- Micro soft J SCRIPT name convert into javascript
- Finaly named ECMA SCRIPT

#  HOISTING
  Compiler:
- Compiled into machine code – 0 s and 1 s
- JIT -> JUST IN TIME
- 1. Automating compilation in java script code
- 2. Execution the java script code like just in time.

# Phases:

- Compilation Phases -> It is only see the Declaration, like another line skip it.
- Excecution phases-> it is excute the value and Assign the value in delclared variable.
- Initialization -> Not memory allocation that is called. Only taken Variable.
- Shadowing -> first prefer in Local varaiable for inside a function same variable name in lexical scope
Concerently

# Stack:
Js Engine only work in call stack -> FILO/LIFO
Queue:
Web apis after time out queue out the content
web Apis:
waiting function and events set timing..

# Promise:
Promise States

1. Pending-> give condition is pending in set of time. Before Calling Resolved
2. Fulfilled-> condition was true . After resolved that is called
3. Rejected-> condition when failed go the other condition.. . reject is call.s

- .then
- .catch
- .finaly
  Promises can use take a real time data in the API get the Inside data




# babel plukin

# ecma documentation

# ecma script history

> es6 after that optional chaining will be released

- object.keys
- object.value
- object.entries

- flat => 2d array convert 1d array => nested array convert a array

> fromEntries() => array to object

# .at() is the array method

# findLast()

# groupBy()

# set methods()

# hositing

# call back

- async callback
- sync callback

- setTimeout, setInterval, eventListners => these all go to web api like async callback

- promises also a async.

.then, .catch, .finally => also promises

resolve = .then
reject = .catch
