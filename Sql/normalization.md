# Normalization

> when we change to normalize the table like avoid updation anomoly is called `normalization` using joins.

# updation anomoly

- like inconsistent data

# deletion anomoly
# insert anomoly

# clause

> avoid anomoly we seperated the table and join the table that is called "Normalization"

# when to seperate and join


# why anomoly ?
> our data will be safe using anomoly

# first normal form rule:(1NF)
> - each column shoud be one information
> - avoid mix data type
> - primary key in every table
> repeating groups are not permitted

# primary key rule
  1. primary key should be unique
  2. primary key should not null value
  3. each table one primary key will be available

# second normal form: (2NF)

> - each non key attribute must depend on the entire primary key


# composite primary key


# foriegn key

> foriegn key is another table primary key


# third normal form: (3NF)

> two non key should not be depended



# inner join

# outer join

- left join
- right join
- full join


#  Aggregation

select * Aggregate function() from tablename

> each keyword in sql question used => Group by

> which ever cloumn repeatly use group by 


# group by

> - dont use where after Group by

# where 
 - where is filter the rows

# having

- After the filter adding another condition using Having

only after group by using "Having" clause



